# PHP Email Validator

email address validator written in php

the validator only checks if an email address is in the correct format, it does not check if the email is real or not.

# how to run

copy all the files in the project folder and paste in in your htdocs folder on xampp or open the folder with any php server
